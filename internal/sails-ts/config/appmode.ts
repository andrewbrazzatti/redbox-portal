import { AppModeConfig } from '@researchdatabox/redbox-core-types';

const appModeConfig: AppModeConfig = {
  bootstrapAlways: true,
  hidePlaceholderPages: true
};

module.exports.appmode = appModeConfig;
